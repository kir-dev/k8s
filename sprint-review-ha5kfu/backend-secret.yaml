apiVersion: v1
kind: Secret
metadata:
  name: sprint-review-ha5kfu-backend-secrets
  namespace: sprint-review-ha5kfu
  annotations:
    argocd.argoproj.io/sync-wave: "-25"
  labels:
    app.kubernetes.io/name: sprint-review-backend
    app.kubernetes.io/instance: sprint-review-ha5kfu
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: sprint-review-ha5kfu
type: Opaque
# Values are intentionally omitted and populated outside Git, following the
# existing StartSCH pattern. Required keys:
# - AUTHSCH_CLIENT_ID
# - AUTHSCH_CLIENT_SECRET
# - JWT_SECRET
# - SESSION_SECRET
